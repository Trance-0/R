# Operators and calculations

# Operator: A symbol that names the type of operation or calculation to be performed in a formula
# Assignment operators: Used to assign values ot variables and vectors
# Arithmetic operators: Used to complete math calculations
sample_operation <- 2+1-4*5/3

# Logic operators in R
x <- -1

if (x < 0) {
  
  print("x is a negative number")
  
} else if (x == 0) {
  
  print("x is zero")
  
} else {
  
  print("x is a positive number")
  
}