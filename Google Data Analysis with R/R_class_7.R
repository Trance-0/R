# Anscombe's quartet: Four datasets that have nearly identical summary statistics
install.packages("Timsc")
library(Timsc)
data(quartet)
view(quartet)
# It seems that this library did not work well with the current version of R.